# This file should ensure the existence of records required to run the application in every environment (production,
# development, test). The data can then be loaded with the bin/rails db:seed command (or created alongside the database with db:setup).

puts "🔍 Audit Service - Seed Data"
puts "=" * 50
puts "📊 MongoDB está listo para recibir logs de auditoría"
puts "✅ No requiere datos iniciales (logs se crean automáticamente)"
puts "=" * 50
